'use strict';
var chai = require('chai');
var expect = chai.expect;
var should = chai.should();
require('dotenv').load();

describe('sample test', function() {
  it('is simple test', function() {
    console.log('hello');
  });
});
